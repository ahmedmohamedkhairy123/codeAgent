# Baseline mode — no codeAgent, pure mini-swe-agent (control group)
agent:
  agent_class: "eval.agents.codeAgent_agent.codeAgentAgent"
  codeAgent_mode: "baseline"
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: "docker"
